[tool.poetry]
name = "Python Pycord Bot"
version = "0.1.0"
description = "A starting point for build in a Discord bot in Python using the pycord library."
authors = ["Darix"]

[tool.poetry.dependencies]
python = "^3.10"
discord = "^2.1.0"
py-cord = "^2.4.0"
xds-protos = "^1.60.0"
Flask = "^3.0.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
